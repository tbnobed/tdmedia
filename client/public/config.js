// Runtime configuration for the Trilogy Digital Media frontend
window.TRILOGY_CONFIG = {
  // Base URL for API requests (empty string means same origin)
  apiBaseUrl: '',
  
  // Application version
  version: '1.0.0',
  
  // Feature flags
  features: {
    analytics: false,
    darkMode: false
  }
};